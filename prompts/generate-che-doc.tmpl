IMPORTANT: You are running unattended. Do NOT use AskUserQuestion at any point.
Skip any user confirmation steps and proceed with your best suggestions automatically.
Skip the build preview entirely.

1. clone git@github.com:eclipse-che/che-docs.git repository
2. Use the /generate-che-docs skill to generate documentation for this PR: {{.PullRequestURL}}
   IMPORTANT: Create branch in eclipse-che/che-docs repository, pull request must be a draft, all commits must be signed.
3. Post the created PR URL as a single new comment on PR {{.PullRequestURL}}

